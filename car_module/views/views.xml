<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="car_module.list">
      <field name="name">car_module list</field>
      <field name="model">car_module.car_module</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="car_module.action_window">
      <field name="name">car_module window</field>
      <field name="res_model">car_module.car_module</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="car_module.action_server">
      <field name="name">car_module server</field>
      <field name="model_id" ref="model_car_module_car_module"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="car_module" id="car_module.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="car_module.menu_1" parent="car_module.menu_root"/>
    <menuitem name="Menu 2" id="car_module.menu_2" parent="car_module.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="car_module.menu_1_list" parent="car_module.menu_1"
              action="car_module.action_window"/>
    <menuitem name="Server to list" id="car_module" parent="car_module.menu_2"
              action="car_module.action_server"/>
    -->
  </data>
</odoo>